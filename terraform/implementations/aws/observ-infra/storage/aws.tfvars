# storage-specific values — observ-infra
# nginx_node_ebs_volume_size_2/_3 and enable_activemq_setup are left at their
# defaults (0/0/false) — observ-infra never provisions postgresql or
# activemq storage, only the 1st (NFS) volume.

nginx_node_ebs_volume_size = 64
