# security has no values specific to itself beyond what's already in
# ../../../profiles/mosip/common.tfvars (cluster_name, aws_provider_region,
# vpc_name, network_cidr, WIREGUARD_CIDR). This file exists for structural
# consistency with every other component under implementations/aws/infra/ —
# intentionally empty until this component ever needs its own value.
