# storage-specific values — eSignet standalone profile

nginx_node_ebs_volume_size   = 200
nginx_node_ebs_volume_size_2 = 0 # PostgreSQL — not needed for standalone eSignet
nginx_node_ebs_volume_size_3 = 0 # ActiveMQ — not needed for standalone eSignet

enable_activemq_setup = false
